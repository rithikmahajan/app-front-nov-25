# 🎨 Authentication & FCM Flow Visual Diagrams

**Date:** October 14, 2025

---

## 🔴 Current Implementation (INCORRECT)

### Apple Sign In - Current Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER TAPS "SIGN IN WITH APPLE"                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    APPLE AUTHENTICATION DIALOG                   │
│  • User approves with Face ID/Touch ID                           │
│  • Apple returns credentials                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              FIREBASE AUTHENTICATION (appleAuthService)          │
│  const credential = auth.AppleAuthProvider.credential(...)       │
│  const userCredential = await auth().signInWithCredential(...)   │
│  ✅ Firebase auth successful                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    GET FIREBASE ID TOKEN                         │
│  const idToken = await user.getIdToken(true)                     │
│  ✅ Firebase ID token obtained                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│            BACKEND AUTHENTICATION (yoraaAPI.firebaseLogin)       │
│  POST /api/auth/login/firebase                                   │
│  Body: { idToken }                                               │
│  ✅ Backend validates Firebase token                             │
│  ✅ Backend returns JWT auth token                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    SAVE TOKEN TO ASYNCSTORAGE                    │
│  await AsyncStorage.setItem('userToken', token)                  │
│  await AsyncStorage.setItem('userData', JSON.stringify(user))    │
│  ✅ Token saved successfully                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    ❌ FCM MISSING!                               │
│  ⚠️ No FCM initialization                                        │
│  ⚠️ No FCM token registration                                    │
│  ⚠️ User CANNOT receive push notifications                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    NAVIGATE TO HOME SCREEN                       │
│  navigation.replace('Home')                                      │
│  ❌ User authenticated but no push notifications                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🟢 Expected Implementation (CORRECT)

### Apple Sign In - Correct Flow (with FCM)

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER TAPS "SIGN IN WITH APPLE"                │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    APPLE AUTHENTICATION DIALOG                   │
│  • User approves with Face ID/Touch ID                           │
│  • Apple returns credentials                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              FIREBASE AUTHENTICATION (appleAuthService)          │
│  const credential = auth.AppleAuthProvider.credential(...)       │
│  const userCredential = await auth().signInWithCredential(...)   │
│  ✅ Firebase auth successful                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    GET FIREBASE ID TOKEN                         │
│  const idToken = await user.getIdToken(true)                     │
│  ✅ Firebase ID token obtained                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│            BACKEND AUTHENTICATION (yoraaAPI.firebaseLogin)       │
│  POST /api/auth/login/firebase                                   │
│  Body: { idToken }                                               │
│  ✅ Backend validates Firebase token                             │
│  ✅ Backend returns JWT auth token                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│          SAVE TOKEN TO ASYNCSTORAGE (CRITICAL ORDER!)            │
│  await AsyncStorage.setItem('userToken', token)  ← SAVE FIRST!   │
│  await AsyncStorage.setItem('userData', JSON.stringify(user))    │
│  ✅ Token saved successfully                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    ✅ INITIALIZE FCM SERVICE                     │
│  const fcmResult = await fcmService.initialize()                 │
│  • Request notification permissions (iOS/Android)                │
│  • Register device for remote messages                           │
│  • Get FCM token from Firebase Messaging                         │
│  ✅ FCM token obtained: eP9K7_LXRkG3...                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ✅ REGISTER FCM TOKEN WITH BACKEND                  │
│  const registerResult = await fcmService                         │
│    .registerTokenWithBackend(authToken)                          │
│                                                                   │
│  POST /api/user/fcm-token                                        │
│  Headers: { Authorization: Bearer <authToken> }                  │
│  Body: { fcmToken, platform: 'ios' or 'android' }               │
│  ✅ FCM token registered on backend                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    NAVIGATE TO HOME SCREEN                       │
│  navigation.replace('Home')                                      │
│  ✅ User fully authenticated                                     │
│  ✅ Can receive push notifications                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete Authentication Flows Comparison

### Side-by-Side Comparison

```
╔═══════════════════════════════╦═══════════════════════════════╗
║    CURRENT (INCORRECT) ❌     ║     EXPECTED (CORRECT) ✅     ║
╠═══════════════════════════════╬═══════════════════════════════╣
║ 1. Apple/Google Sign In       ║ 1. Apple/Google Sign In       ║
║ 2. Firebase Authentication    ║ 2. Firebase Authentication    ║
║ 3. Get Firebase ID Token      ║ 3. Get Firebase ID Token      ║
║ 4. Backend Auth               ║ 4. Backend Auth               ║
║ 5. Save Auth Token            ║ 5. Save Auth Token            ║
║ 6. ❌ No FCM Setup            ║ 6. ✅ Initialize FCM          ║
║ 7. Navigate Home              ║ 7. ✅ Register FCM Token      ║
║                               ║ 8. Navigate Home              ║
║                               ║                               ║
║ Result:                       ║ Result:                       ║
║ ❌ No push notifications      ║ ✅ Push notifications work    ║
╚═══════════════════════════════╩═══════════════════════════════╝
```

---

## 🚪 Logout Flow Comparison

### Current Logout (INCOMPLETE)

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER TAPS LOGOUT BUTTON                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    CLEAR ASYNCSTORAGE                            │
│  await AsyncStorage.clear()                                      │
│  ✅ Local data cleared                                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FIREBASE SIGN OUT                             │
│  await auth().signOut()                                          │
│  ✅ Firebase session ended                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    ❌ FCM TOKEN CLEANUP MISSING!                 │
│  ⚠️ FCM token still registered on backend                        │
│  ⚠️ User will CONTINUE to receive notifications                  │
│  ⚠️ Privacy/security issue!                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    NAVIGATE TO WELCOME                           │
│  navigation.reset({ index: 0, routes: [{ name: 'Welcome' }] })  │
│  ❌ User logged out but still gets notifications!                │
└─────────────────────────────────────────────────────────────────┘
```

### Expected Logout (COMPLETE)

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER TAPS LOGOUT BUTTON                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                ✅ UNREGISTER FCM TOKEN (CRITICAL!)               │
│  const token = await AsyncStorage.getItem('userToken')           │
│  await fcmService.unregisterTokenFromBackend(token)              │
│                                                                   │
│  DELETE /api/user/fcm-token                                      │
│  Headers: { Authorization: Bearer <token> }                      │
│  ✅ FCM token removed from backend                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FIREBASE SIGN OUT                             │
│  await auth().signOut()                                          │
│  ✅ Firebase session ended                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    GOOGLE SIGN OUT (if applicable)               │
│  const isSignedIn = await GoogleSignin.isSignedIn()              │
│  if (isSignedIn) {                                               │
│    await GoogleSignin.signOut()                                  │
│  }                                                                │
│  ✅ Google session ended                                         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    CLEAR ALL ASYNCSTORAGE                        │
│  await AsyncStorage.multiRemove([                                │
│    'userToken', 'userData', 'fcmToken',                          │
│    'guestSessionId', 'wishlistItems'                             │
│  ])                                                               │
│  ✅ All local data cleared                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    CLEAR CONTEXT STATES                          │
│  await clearCart()  // CartContext                               │
│  await clearWishlist()  // WishlistContext                       │
│  ✅ App state reset                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    NAVIGATE TO WELCOME                           │
│  navigation.reset({ index: 0, routes: [{ name: 'Welcome' }] })  │
│  ✅ User fully logged out                                        │
│  ✅ Will NOT receive push notifications                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📱 FCM Token Lifecycle

### Complete Token Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                         APP LAUNCH                               │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST PERMISSIONS                           │
│  iOS: messaging().requestPermission()                            │
│  Android: PermissionsAndroid.request(POST_NOTIFICATIONS)         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    GET FCM TOKEN                                 │
│  await messaging().getToken()                                    │
│  Save to AsyncStorage('fcmToken')                                │
│  ⚠️ Don't register with backend yet (no auth token)              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────┴─────────┐
                    │                   │
              GUEST USER          AUTHENTICATED USER
                    │                   │
                    ↓                   ↓
        ┌───────────────────┐  ┌───────────────────┐
        │ No backend        │  │ Has auth token    │
        │ registration      │  │ from storage      │
        │                   │  │                   │
        │ FCM token stays   │  │ Register FCM      │
        │ local only        │  │ with backend      │
        └───────────────────┘  └───────────────────┘
                    │                   │
                    │                   ↓
                    │      ┌──────────────────────────┐
                    │      │ POST /api/user/fcm-token │
                    │      │ ✅ Token registered      │
                    │      └──────────────────────────┘
                    │                   │
                    └──────┬────────────┘
                           ↓
              ┌────────────────────────┐
              │   USER LOGS IN         │
              └────────────────────────┘
                           ↓
              ┌────────────────────────┐
              │ ✅ REGISTER FCM TOKEN  │
              │ with backend           │
              └────────────────────────┘
                           ↓
              ┌────────────────────────┐
              │ User receives          │
              │ push notifications     │
              └────────────────────────┘
                           ↓
                    (User session)
                           ↓
              ┌────────────────────────┐
              │   USER LOGS OUT        │
              └────────────────────────┘
                           ↓
              ┌────────────────────────┐
              │ ✅ UNREGISTER FCM      │
              │ TOKEN from backend     │
              └────────────────────────┘
                           ↓
              ┌────────────────────────┐
              │ FCM token removed      │
              │ No more notifications  │
              └────────────────────────┘
```

---

## 🔐 Token Storage Order (CRITICAL!)

### ❌ Wrong Order (Race Condition)

```
┌─────────────────────────────────────────────────────────────────┐
│              Backend Auth Success (yoraaAPI.firebaseLogin)       │
│              Returns: { token, user }                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ❌ INITIALIZE FCM FIRST (WRONG!)                    │
│              const fcmResult = await fcmService.initialize()     │
│              ⚠️ This might trigger app state change              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ❌ TRY TO REGISTER FCM TOKEN                        │
│              await fcmService.registerTokenWithBackend()         │
│              ⚠️ Needs auth token but not saved yet!              │
│              ❌ REGISTRATION FAILS!                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              Save Auth Token (too late)                          │
│              await AsyncStorage.setItem('userToken', token)      │
└─────────────────────────────────────────────────────────────────┘
```

### ✅ Correct Order

```
┌─────────────────────────────────────────────────────────────────┐
│              Backend Auth Success (yoraaAPI.firebaseLogin)       │
│              Returns: { token, user }                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ✅ SAVE AUTH TOKEN FIRST (CRITICAL!)                │
│              await AsyncStorage.setItem('userToken', token)      │
│              await AsyncStorage.setItem('userData', user)        │
│              ✅ Token saved and available                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ✅ NOW INITIALIZE FCM                               │
│              const fcmResult = await fcmService.initialize()     │
│              ✅ Safe to proceed                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              ✅ REGISTER FCM TOKEN WITH BACKEND                  │
│              const authToken = await AsyncStorage                │
│                .getItem('userToken')  ← Token is available!      │
│              await fcmService.registerTokenWithBackend(authToken)│
│              ✅ REGISTRATION SUCCESS!                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 All Authentication Methods Flow

### Unified Flow (All Auth Methods)

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION METHOD                         │
│  • Apple Sign In                                                 │
│  • Google Sign In                                                │
│  • Phone + OTP                                                   │
│  • Email + Password (Login)                                      │
│  • Email + Password (Signup)                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              FIREBASE AUTHENTICATION                             │
│  Different methods, same Firebase Auth                           │
│  ✅ Firebase user created/authenticated                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              GET FIREBASE ID TOKEN                               │
│  const idToken = await user.getIdToken()                         │
│  ✅ Firebase ID token obtained                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              BACKEND AUTHENTICATION                              │
│  await yoraaAPI.firebaseLogin(idToken)                           │
│  Backend verifies Firebase token                                 │
│  Returns JWT auth token                                          │
│  ✅ Backend auth successful                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────┐
        │   CRITICAL SECTION (Order Matters!)   │
        └───────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              STEP 1: SAVE AUTH TOKEN                             │
│  await AsyncStorage.setItem('userToken', token)                  │
│  await AsyncStorage.setItem('userData', user)                    │
│  ✅ Token saved - available for all operations                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              STEP 2: INITIALIZE FCM                              │
│  const fcmResult = await fcmService.initialize()                 │
│  • Request permissions                                           │
│  • Get FCM token                                                 │
│  ✅ FCM initialized                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              STEP 3: REGISTER FCM TOKEN                          │
│  const authToken = await AsyncStorage.getItem('userToken')       │
│  await fcmService.registerTokenWithBackend(authToken)            │
│  ✅ FCM token registered on backend                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              STEP 4: NAVIGATE TO HOME                            │
│  navigation.replace('Home')                                      │
│  ✅ User fully authenticated                                     │
│  ✅ Can receive push notifications                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Decision Tree: Which Service to Use?

```
                    Need to Implement Auth?
                              │
                              ↓
           ┌──────────────────┴──────────────────┐
           │                                     │
           ↓                                     ↓
    New Implementation               Fixing Existing Code
           │                                     │
           ↓                                     ↓
    ┌──────────────────┐              ┌──────────────────┐
    │ Use Unified      │              │ Two Options:     │
    │ Service ✅       │              │                  │
    │                  │              │ A) Migrate to    │
    │ authenticationSe │              │    Unified (✅)  │
    │ rvice.js         │              │                  │
    │                  │              │ B) Add FCM to    │
    │ • All methods    │              │    existing (⚠️) │
    │ • FCM included   │              └──────────────────┘
    │ • Maintained     │                       │
    │ • Clean code     │                       ↓
    └──────────────────┘           Choose Option A (Recommended)
           │                                     │
           └─────────────────┬───────────────────┘
                             ↓
               ┌──────────────────────────────┐
               │ Result: Working Auth + FCM   │
               │ ✅ Push notifications work   │
               │ ✅ Clean, maintainable code  │
               └──────────────────────────────┘
```

---

## 🎯 Visual Summary: What to Fix

```
╔═══════════════════════════════════════════════════════════════════╗
║                  CRITICAL FIXES NEEDED                            ║
╠═══════════════════════════════════════════════════════════════════╣
║                                                                   ║
║  Current State:                                                   ║
║  ┌─────────────┐                                                 ║
║  │ Auth Works  │ ✅                                               ║
║  └─────────────┘                                                 ║
║  ┌─────────────┐                                                 ║
║  │ FCM Missing │ ❌ ← FIX THIS!                                   ║
║  └─────────────┘                                                 ║
║                                                                   ║
║  Add After Authentication:                                        ║
║  ┌──────────────────────────────────────────────┐               ║
║  │ 1. Save token ✅ (already done)               │               ║
║  │ 2. Initialize FCM ← ADD THIS                 │               ║
║  │ 3. Register FCM token ← ADD THIS             │               ║
║  └──────────────────────────────────────────────┘               ║
║                                                                   ║
║  Add to Logout:                                                   ║
║  ┌──────────────────────────────────────────────┐               ║
║  │ 1. Unregister FCM token ← ADD THIS           │               ║
║  │ 2. Sign out Firebase ✅ (already done)       │               ║
║  │ 3. Clear storage ✅ (already done)           │               ║
║  └──────────────────────────────────────────────┘               ║
║                                                                   ║
║  Estimated Time: 2-4 hours                                        ║
║  Complexity: Medium                                               ║
║  Impact: HIGH (enables push notifications)                        ║
║                                                                   ║
╚═══════════════════════════════════════════════════════════════════╝
```

---

## 📝 Quick Reference: Code Locations

### Files That Need FCM Integration:

```
1. /src/services/appleAuthService.js
   └─ Line ~176: After yoraaAPI.firebaseLogin()
      ✚ Add FCM initialization
      ✚ Add FCM registration

2. /src/services/googleAuthService.js
   └─ Line ~178: After yoraaAPI.firebaseLogin()
      ✚ Add FCM initialization
      ✚ Add FCM registration

3. /src/screens/loginaccountmobilenumberverificationcode.js
   └─ Line ~101: After yoraaAPI.firebaseLogin()
      ✚ Add FCM initialization
      ✚ Add FCM registration

4. Profile/Settings Screen (wherever logout is)
   └─ In logout function:
      ✚ Add FCM unregistration (FIRST step)
      ✚ Then existing logout code
```

### Alternative: Use Unified Service

```
/src/services/authenticationService.js
   └─ Already has FCM integration! ✅
   └─ Just migrate screens to use this service
   └─ Remove old service files
```

---

**Remember:** Your `authenticationService.js` already has the correct implementation with FCM! The quickest fix is to migrate your screens to use it instead of adding FCM to multiple files.

